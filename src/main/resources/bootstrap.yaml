spring:
  profiles:
    active: dev
  cloud.config:
    profile: dev
    enabled: true
    fail-fast: true
    uri: http://localhost:8888
  application.name: discovery-server
logging:
  level:
    root: info
server.port: 8761